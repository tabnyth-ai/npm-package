{
  "name": "@tabnyth/postgres",
  "version": "0.2.0",
  "description": "Postgres adapter for Tabnyth Studio.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "tabnyth": "0.2.0",
    "tsup": "^8.3.5"
  },
  "peerDependencies": {
    "tabnyth": "^0.2.0"
  },
  "engines": {
    "node": ">=20.11"
  }
}
